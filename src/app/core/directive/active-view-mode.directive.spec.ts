import { ActiveViewModeDirective } from './active-view-mode.directive';

describe('ActiveViewModeDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveViewModeDirective();
    expect(directive).toBeTruthy();
  });
});
